<article class="comment">
  <span class="profile">
    <%= avatar(comment.user, size: 50) if comment.user.avatar.attached? %> 
    <span class="username"><%= comment.user.username %></span>
  </span>
  <%= simple_format(comment.content) %>
</article>